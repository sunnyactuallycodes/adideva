{
  "name": "bookmyindia-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm run build --prefix frontend && rm -rf dist && cp -r frontend/dist ./dist",
    "dev": "npm run dev --prefix frontend"
  }
}
